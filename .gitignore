# gitignore template for LangChain products, e.g., LangGraph, LangSmith
# website: https://www.langchain.com/
# website: https://www.langchain.com/langgraph

# LangGraph
.langgraph_api/

# Dependencies
node_modules/
package-lock.json

# Cache directory (but keep creditUsage.json)
backend/db/cache/

# Test scripts
test-*.js

# Environment variables
.env
.env.local
.env.production

# Local LLM credentials — NEVER commit
.llm-credentials.local.json

# Build output (per-location so vendored upstream sources — e.g. Blender's
# intern/libmv/libmv/build/build_config.h — are not accidentally excluded)
/dist/
frontend/dist/
/build/
.vite/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.backup
.serverless/
lambda-deployment.zip
*.zip
.serverless/
lambda-*.zip
test-results/
dist-electron/
ge9x-deliverable/
autonomous-output/
engine-output/

# E2E artefacts (regenerated on every run; never commit)
e2e-output/
e2e/screenshots/

# Large local reference media — never commit (course videos in repo root)
*.mp4
*.webm
*.mov

# AI-orchestration scratch / wakeup notes
docs/superpowers/notes/custom-occt-build-recon.md
