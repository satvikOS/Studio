# ArchDisc Studio ↔ DCC Tool Parity Map

Studio's target is full **Blender** parity as the base (see
`BLENDER_KERNEL_MAP.md`) + game-engine parity (`GAME_ENGINE_PARITY_MAP.md`),
and on top of that **1:1-or-better parity** with the major DCC tools below.

This map is an **honest coverage tracker** (status as of 2026-05-29). It is the
counterpart to the other two maps for the non-Blender DCC tools, which had no
tracking before. Status legend:

- **DONE** — a real implementation exists and is verified by a headed e2e.
- **PARTIAL** — a real but limited/approximate implementation exists.
- **STUB** — a placeholder (counter / marker geometry) — not real yet.
- **ABSENT** — no implementation.

Every parity slice should cite the tool concept in code comments + ribbon
tooltips, mirror the tool's algorithm + UX, and update this table.

## ZBrush / Mudbox / Nomad (digital sculpting)

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Localized brush sculpt (draw/inflate/crease/pinch/flatten/grab/smooth) | DONE | slice 758 — `v3/zbrushdetail/`: the 7 canonical kernels (draw/inflate/crease/pinch/flatten/grab/smooth) are individually addressable as PURE per-vertex displacement functions in `brushKernels.js` (each takes `(vert, normal, strength, falloff, ctx?)` and returns `[dx,dy,dz]`). Five canonical FALLOFF CURVES (`falloffCurves.js`: linear / smooth / sphere / sharp / constant) drive the radial weight `t∈[0..1]` per vertex; matches ZBrush's "Brush > Falloff" + Blender's "Custom Curve" surface. The installer wires four ops onto window: `__studioZBrushBrush(meshUuid, brush, params)` resolves a mesh by uuid (or active selection), walks every vertex within `params.radius` of `params.center`, evaluates the active curve, and applies the chosen kernel; `__studioZBrushSetFalloffCurve(curve)` switches the active curve; `__studioZBrushGetActiveSettings()` reports current state + kernel/curve catalogues; and `__studioZBrushBrushVerifyAll(meshUuid)` is the SMOKE test that runs every kernel once against a snapshot of the mesh, returning per-mode changed-vertex counts — the proof each mode is individually addressable and does real work (this is what flips the row from PARTIAL → DONE). All ops register under category 'sculpt' alongside DynaMesh / SubTools / Polypaint / Sculpt Layers. The legacy `paintBrushAt` localized brush + X-symmetry remain unchanged for the live viewport stroke; this slice adds the verifiable per-mode kernel surface those modes were previously locked inside. |
| Erosion / weathering / degradation | DONE | `sculptErode` / `sculptWeather` (deterministic fBm) |
| Masking (mask buffer) | DONE | real per-vertex protect-mask: `paintMaskAt` + `mask` brush mode; the brush restores masked verts post-stroke; masked areas shade darker; Clear/Invert (slice 151) |
| Sculpt layers (multi-res levels) | DONE | TWO systems: (1) sculpt LAYERS — `sculpt/layers.js` per-vertex delta layers with live strength + visibility + active + merge-down + recomposite (the composited result = base + Σ layer.delta × strength × visible). (2) MULTI-RESOLUTION levels (slice 733, ZBrush SubDiv/HD Geometry, Mudbox subdivision levels, Blender Multires) — `subdiv/multires.js`: a level stack where subdivideUp() Loop-subdivides the top level into a finer one, setLevel(L) rebuilds from the base re-adding each level's stored per-vertex DETAIL displacement, and bake() captures a sculpt into the active level (base edits at L0, detail = current − subdivision-prediction at L>0). Stepping DOWN to edit low-frequency form and back UP PRESERVES the high-frequency detail. Detail is stored in TANGENT SPACE (slice 735 — coefficients along the smooth surface's normal/tangent/bitangent) so it RIDES the surface when a lower level rotates or deforms (e2e: a bump sculpted at L2 still points along the surface normal after the base is rotated 90°, dot≈1.0). `__studioMultires*` ops in global search; headed e2e proves a bump sculpted at L2 survives a L0→L2 round-trip (dx=0.012).
| DynaMesh (uniform reskin) | DONE | `dynaMeshGeometry` — voxelise (ray-crossing point-in-mesh) -> watertight cuberille -> weld -> Laplacian smooth; uniform-topology reskin (slice 155) |
| ZRemesher (quad retopology) | DONE | TWO modes: (1) UNIFORM quad remesh of any mesh — `quadRemeshGeometry` voxelise -> 4-sided quads -> weld -> smooth (slice 164); (2) FIELD-ALIGNED quad remesh (slice 177, ZBrush ZRemesher / Instant Meshes) — `remesh/fieldAlignedQuad.js` estimates the principal-curvature cross-field (real fundamental forms -> shape operator -> eigen-directions), RoSy-smooths it, and traces a field-following streamline net so quad edges FLOW with curvature (diagonal-wave surface -> ~45deg flow, cylinder -> hoop/axial; alignment ~1.0). "ZRemesh" ribbon + visible quad wireframe + `__studioFieldQuadRemesh`. Honest scope: parametric (disk-topology) surfaces; singularity-aware integer-grid extraction on arbitrary genus is the remaining frontier |
| Polypaint (vertex paint by brush) | DONE | `paintPolyAt` — real per-vertex colour brush with radial falloff; `polypaint` brush mode + colour picker (slice 161) |
| Stamp / stencil / alpha brushes | DONE | `stamp` brush mode — procedural alpha (concentric rings) drives a patterned relief, not a uniform dome (slice 163). Custom alpha-image stencils are the next extension |
| UV Master (one-click unwrap) | DONE | slice 734 — REAL LSCM (Least Squares Conformal Maps, Lévy et al. 2002 — the unwrap Blender/Maya/Headus use). `zuvmaster/lscm.js`: assembles the per-triangle Cauchy-Riemann conformal equations in each triangle's local 2D frame, pins the two farthest-apart vertices to fix the similarity gauge, and solves the over-determined sparse system in the least-squares sense via matrix-free conjugate-gradient on the normal equations (dependency-free). `unwrapAll` runs LSCM as the primary method; `__studioZUVMasterDistortion` reports mean angle distortion (tilted plane 0.00001° vs 54° old projection). Slice 736 adds AUTO SEAM-CUTTING (`zuvmaster/seams.js`, Blender Smart UV Project / Maya Automatic): detects sharp-dihedral-angle + boundary edges as seams, flood-fills triangles into developable charts, rebuilds each chart with its OWN vertex copies (a real cut — shared corners are duplicated), LSCM-flattens each, and lays them out. A closed welded box → 6 charts at 0.000003° distortion vs 25° for a single whole-mesh LSCM. `__studioZUVMasterUnwrapWithSeams` / `__studioZUVMasterChartCount`. Honest scope: angle-threshold seams (no user seam-painting yet). |
| Subtools / subtool hierarchy | DONE | slice 732 — ZBrush SubTool manager: an ordered list of independent sculptable meshes (each a scene primitive) with one ACTIVE subtool + a SOLO flag. Ops: list / setActive / rename / setVisible / solo (hide the rest) / append (new primitive) / duplicate (geometry+transform) / delete / move (reorder) / merge-down (collapse into the one below) / merge-visible (all visible → one mesh via mergeGeometries with world transforms baked). Auto-syncs to the live scene so meshes spawned by other tools appear as subtools, and active-subtool selection drives `__studioSelectMesh` so sculpt/edit ops target it. `__studioSubtool*` ops in global search; Inspector shows active subtool kind/verts/tris/mass. Headed e2e: append 3 → solo → duplicate → reorder → merge-down → merge-visible → single mesh (3084 tris). (ZBrush SubTools / Mudbox layers / Nomad subtools.) |

## Houdini (procedural)

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Procedural node graph (geometry nodes / SOP DAG) | DONE | real geometry node graph (nodegraph/): DAG eval engine + visual editor (draggable nodes, SVG wires, click-to-connect, params, Evaluate->Scene). Nodes: primitive/transform/subdivide/bevel/displace/array/merge/output (slice 152). Material/shader graph (slice 165) + Blueprint exec-flow graph (slice 168) both reuse this framework, generalized to a node-type registry. **Slice 757 — Geometry Nodes batch 2 (`v3/geomnodes2/`)** ships five Blender-flavour SOPs as direct window ops on top of the existing DAG: `__studioGN2InstanceOnPoints(sourceUuid, points, opts)` (THREE.InstancedMesh of a source at each `{pos,normal}` point with normal-aligned orientation + composed source TRS), `__studioGN2RealizeInstances(instancedUuid)` (bakes an InstancedMesh into a single merged BufferGeometry — concatenated, normal-matrix-corrected, ready for CSG/remesh), `__studioGN2MergeByDistance(meshUuid, tolerance)` (O(N) spatial-hash vertex weld with 27-cell neighbour search + degenerate-tri pruning), `__studioGN2TransformGeometry(meshUuid, matrix4Array)` (bakes a Matrix4 into the position attribute + rotates normals through the normal matrix; mesh.position/rotation/scale untouched), `__studioGN2Subdivide(meshUuid, levels)` (delegates to `v3/catmullclark/__studioCatmullClark` when a quad cage is recorded, else `common/subdivide.js` loopSubdivide). |
| VEX-like expressions | DONE | slice 745 — real Houdini-VEX flavour wrangle (`v3/vex/vexLexer.js` + `vexParser.js` + `vexInterpreter.js` + `vexwrangle.js`). Typed locals (`float`/`int`/`vec3`), full attribute model (`@P @N @Cd @id @pt @npt` + `.x/.y/.z/.r/.g/.b` swizzle), `{x,y,z}` vec3 literal, compound assigns `+= -= *= /=`, `if/else/return`, `&&` / `\|\|` / `!`, and a curated built-in set (sin/cos/length/normalize/clamp/mix/dot/cross/fit/fit01/noise — deterministic value noise, no Math.random). Pure JS, zero deps, NO eval / `new Function` — hand-written recursive-descent parser + AST walker. Auto-creates a `color` attribute on first `@Cd` write. New ops: `__studioVEXCompile / Run / RunOnMesh / Builtins / Attrs`. Headed e2e on a 32×32 plane proves `@P.y += sin(@P.x*3.14); @Cd = {1,0,0}` moves Y by the exact sinusoid (16 sampled pts, 5-decimal closeTo), leaves X/Z untouched, paints Cd red; plus a parse-error path + `normalize(@N)` displacement test. |
| Scatter / distribute points | DONE | Real Bridson Poisson-disk dart-throwing on triangle meshes (`v3/scatter/poissonSurface.js`). Area-weighted triangle CDF + spatial-hash neighbour scan + density-mask gate (named masks, JSON-serialisable across the test boundary). Deterministic (mulberry32-seeded — NEVER Math.random). Ops: __studioScatterOnSurface / OnSurfaceInstanced / List / Delete / MaskList. |
| Cell fracture / explode | DONE | `MOD_explode` cell fracture |
| Rigid body dynamics (RBD) | DONE | real rigid-body solver (slice 166) — semi-implicit Euler, 3D momentum + gravity, sphere-proxy pairwise collision with impulse resolution + positional correction (bodies stack/settle, no interpenetration), mass-by-volume, ground + contact friction. Drives the VFX/Sim "Drop" tool; `__studioPhysicsStep`/`__studioPhysicsState` (also Unreal Chaos / Unity PhysX / Blender Rigid Body World). Sphere-proxy collision is the honest scope (not full convex) |
| Volume / VDB | DONE | slices 730–731 — Eulerian GAS + FIRE SIMULATION (Houdini Pyro FX / Blender Mantaflow / FumeFX / EmberGen): Stam stable-fluids solver on the slice-698 volume grid (`volume/pyro.js`) — semi-Lagrangian advect + Gauss-Seidel pressure-projection + buoyancy + vorticity confinement + dissipation/cooling (slice 730). Slice 731 adds COMBUSTION: a fuel field that ignites above an ignition temperature, releasing heat (fire) + soot (smoke density) + a volumetric expansion (divergence source feeding the projection) and is consumed; pilot-lit fuel jets self-sustain. The reaction-rate 'flame' field writes to the texture B channel and the raymarch shader emits an incandescent red→orange→yellow→white fire ramp. `__studioPyro*` ops + one-shot `__studioPyroIgnite` (smoke) / `__studioPyroCampfire` (fire). Headed e2e: plume rises (730); combustion self-ignites from a pilot light, produces smoke from fuel alone, fire exceeds ignition temp, flame written to texture (731). Slice 751 — sparse VDB-style top-level dense root + fixed-size leaf tiles (default 8³), allocated on first nonzero write and pruned by per-tile max. Numeric packed key (signed 21-bit per axis → Number-safe ≤2^53). Ops: `__studioVDBCreate` / `Set` / `Get` / `Stats` / `Prune` / `Clear` / `List` / `ForEachTile`. The dense pyro solver from slices 730–731 is untouched — sparseVDB is a parallel backing store; a `__studioVDBFromPyro` helper for read-only import will follow in a later slice |

## Maya

| Capability | Status | Notes / next |
|------------|--------|--------------|
| NURBS surfaces/curves | DONE | rational degree-3 tensor-product NURBS SURFACE (slice 153) + rational degree-3 NURBS CURVE (slice 179) + TRIMMED surfaces (slice 178) + SOLID SEWN B-REP BOOLEANS (slice 181, `brep/occtBoolean.js` — real OCCT kernel via opencascade.js, LAZY dynamic import so the main bundle stays small; cut/fuse/common produce exact NURBS-trimmed solids; the box-minus-cylinder cut yields the correct 7-face topology (6 box faces + 1 cylindrical inner wall)). "B-rep Bool" ribbon + `__studioBRepBoolean` |
| Deformer stack / node editor | DONE | non-destructive modifier stack (slice 154) + the geometry node editor (slice 152) |
| MASH / instancing | DONE | Real MASH arrange/distribute/effector pipeline (`v3/mash/distribute.js` + `effector.js` + `arrange.js`). Five distribute modes (linear/grid/radial/spiral/fibonacci); InstancedMesh-backed replicate; effector stack (sine/noise/falloff/random/step) re-bases from baseMatrices for deterministic stacking. New ops: __studioMASHDistribute / Replicate / Effector / List / Remove. |
| Rigging (joints / IK / constraints / skinning) | DONE | Slice 754 adds the missing skinning workflow on top of the existing armature/IK/constraints stack: __studioSkinAutoWeight (segment-distance falloff, quadratic radius dropoff, top-4 per vertex, 2-pass mesh-graph Laplacian smoothing of the weight vectors as a heat-diffusion approximation) + __studioSkinBindMesh (auto-weight then Mesh→SkinnedMesh swap + bind) + __studioSkinPose (Maya-style by-name bone rotation) + __studioSkinUnbind. Headed e2e proves a 2-bone armature on a 3m cylinder: top-bone 45° rotation moves upper-half verts >0.2m on average while lower-half stay <0.01m (real per-vertex LBS, not rigid translate). |
| Timeline / Sequencer (keyframe track editor) | DONE | native monotone Sequencer dock (slice 167, Unreal Sequencer / Unity Timeline / Blender Dope Sheet) — frame ruler, draggable playhead/scrubber, transport, one track per animated object with keyframe diamonds; wired to the keyframe engine which now interpolates position + rotation + SCALE with linear/bezier/constant easing. Replaces the inherited blue display-only TimelineEditor |
| Mesh edit (MultiCut / Bridge edges / Bridge faces / Bevel edges) | DONE | slice 756 — Maya MultiCut + Bridge edges + Bevel edges trio (`v3/meshedit/`). `bridgeEdges.js` takes two equal-length parallel border edge loops loopA/loopB and stitches N quads connecting (loopA[i],loopA[j],loopB[j],loopB[i]) with auto winding-flip via centroid-to-centroid dot test; `multiCut.js` builds the cut plane (line × avg-surface-normal) through (verts[startIdx]→verts[endIdx]), inserts `segments-1` evenly-spaced intermediate verts on the line, and splits any tri the plane crosses into three sub-tris (apex/newP/newQ + newP/p/q + newP/q/newQ — same scheme editKnife uses), with edge-crossing weld cache so shared edges don't dupe; `bevelEdges.js` replaces each marked edge with two parallel edges offset by `width` along each adjacent wing's surface (`n × e` flipped to point INTO the wing), re-wires the two wing tris to the offset verts, and fills the gap with a quad strip — only manifold edges (2 wings) are beveled, boundary edges are skipped. Five new ops: `__studioMeshBridge` / `__studioMeshMultiCut` / `__studioMeshBevelEdges` / `__studioMeshExtrudeEdge` (extrude triangle's first edge along axis × distance with a connecting quad) / `__studioMeshDeleteFace`. Auto-registered via `v3/meshedit/`. Pure JS, no new deps. Honest scope: per-edge bevel (no chained corner resolution yet — Maya's chained-edge fillet is the next slice). |

## 3ds Max

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Modifier stack (non-destructive, reorderable) | DONE | real live stack re-evaluated from a clean base every edit (reuses the node engine); add/remove/reorder/toggle; removing a mid-stack modifier truly reverts it (slice 154) |
| Spline → Loft / sweep along path | DONE | Loft/Sweep (slice 170, 3ds Max Loft / Rhino Sweep1) — sweeps an arbitrary polygonal profile (square/L/star/n-gon) along a path curve (helix/arc/S-curve/closed ring) via Frenet frames into a swept surface; "Loft/Sweep" primitive + `__studioSweepLoft`. (Path drives a built-in curve set; sweep along a user-drawn curve is the next step.) |
| Parametric primitives (re-editable) | DONE | real re-editable parametric primitives (slice 750) — every primitive carries userData.archdiscStudioPrimitiveParams + archdiscStudioPrimitiveParamSchema; rebuild() swaps mesh.geometry from a fresh BufferGeometry while preserving transform/material/userData/selection. 20 primitive kinds covered (cube/sphere/plane/cylinder/cone/torus/icosahedron/dodecahedron/tetrahedron/torus-knot + voxel/suzanne/teapot/color-cube/arch/ogee/spline-* bag kinds). Ops: __studioPrimitive{Get,Set,Reset,GetSchema}Params. |

## Cinema 4D

| Capability | Status | Notes / next |
|------------|--------|--------------|
| MoGraph cloner + effectors | DONE | `mographCloner` — grid clones (one InstancedMesh) + radial/linear falloff effector driving scale/rise/twist (slice 149) |
| Deformers (bend/twist/taper/…) | DONE | Simple Deform family (Blender ports) |

## Substance (Painter / Designer)

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Procedural material graph | DONE | node-based MATERIAL/SHADER graph (slice 165) — reuses the geometry DAG engine retargeted to PBR: Texture (deterministic Voronoi/noise/checker/wave/gradient -> CanvasTexture) / Color / Scalar / Color Mix nodes feed a Material Output node that assembles a MeshStandardMaterial (colour / roughness / metalness / emissive / map channels), applied to the selected mesh. Visual editor + `__studioApplyMaterialGraph` hook. (Substance Designer / Unreal Material Editor / Unity Shader Graph.) |
| PBR texture painting on UVs | DONE | full PBR channel set — paint colour / roughness / metalness / emissive / HEIGHT, each its own CanvasTexture wired to the matching MeshStandardMaterial map; channel selector + colour picker + `texpaint` brush mode (slices 156, 160). Height paints a bumpMap; a Sobel "Height -> Normal" bake derives a tangent-space normalMap (slice 169) — completes the Substance channel set |
| Map baking (AO/normal/position) | DONE | slice 746 — REAL UV-baked Substance/Marmoset map trio. `bake/aoTexture.js` (slice 284) was already a per-texel UV-grid hemisphere ray-trace into `material.aoMap`; this slice ships the matching sibling bakes. `bake/normalTexture.js` interpolates the world-space surface normal via barycentric across the UV-containing triangle and encodes it as an OBJECT-space normal map (`n*0.5+0.5` per axis → RGB), wires the CanvasTexture into `material.normalMap`. Object-space chosen over tangent-space because a SELF-bake under tangent-space gives the useless identity `(0,0,1)` at every texel; object-space records the actual surface direction (top-face `(128,255,128)`, +X face `(255,128,128)`). `bake/positionTexture.js` writes the world position normalised into `mesh.boundingBox` (R=x, G=y, B=z) and stores the CanvasTexture + bbox decode on `userData.archdiscStudioPositionMap` (debug/source data, not a shader channel). New ops `__studioBakeAOMap` / `__studioBakeNormalMap` / `__studioBakePositionMap` + matching `AO Map / N Map / Pos Map` ribbon buttons next to the vertex-colour `Bake AO / Bake N / Bake Pos` (canonical home pairs vertex bake vs texture bake). The slice-699 `Math.random()`-driven proxy stays where it sits — these op surfaces are the real implementation. Pure JS, no new deps. |
| SD material-builder nodes (Normal/AO from Height) | DONE | slice 739 — sdesigner graph gains `normalfromheight` (Sobel gradient over the sampled height field → tangent-space normal n=normalize(-dHdx·str,-dHdy·str,1) encoded n·0.5+0.5; flat→(.5,.5,1); flipY swaps GL/DX) and `aofromheight` (concave occlusion: 1−(avgNeighbour−centre)·str). The two flagship Substance Designer "Normal" / "Ambient Occlusion" output nodes. e2e: flat=(.5,.5,1), 300/300 blue-dominant valid normals, AO∈[0.83,1.0]. `__studioSDesignerApply('normalfromheight'|'aofromheight',…)`. |
| HumanIK mirror pose + reset to bind | DONE | slice 740 — `humanik/autorig.js` gains `mirrorPose` (reflect the pose across the sagittal plane: snapshot quats, then swap each Left↔Right bone with quaternion mirror q→(x,−y,−z,w); centre bones self-reflect) and `resetToBind` (clear all bone rotations → T-pose). The two daily Maya HumanIK / MotionBuilder / Cascadeur pose ops. e2e: LeftArm z=0.296 → mirror → RightArm z=−0.296 & LeftArm z=0; reset → 22 bones to identity (maxDev 0). `__studioHumanIKMirrorPose` / `__studioHumanIKResetToBind`. |

## Rhino

| Capability | Status | Notes / next |
|------------|--------|--------------|
| NURBS curves / surfaces | DONE | NURBS surface (slice 153) + NURBS CURVE (slice 179) + TRIMMED surfaces (slice 178) + SOLID SEWN B-REP BOOLEANS (slice 181, OCCT via opencascade.js, lazy-loaded). The exact-solid-modelling stack is complete |
| SubD | DONE | slice 749 — true quad Catmull-Clark subdivider (`v3/catmullclark/catmullclark.js`) with the canonical face/edge/vertex weights, proper smooth-boundary ring + valence-2 corner-hold, **Hoppe-94 semi-sharp crease decay** (per step new edge sharpness = `max(0, s-1)`; sharp edges hold the midpoint; crease verts use the `(P_prev + 6P + P_next)/8` rule; 3+ incident sharp edges produce held corners). The mesh's QUAD CAGE is stored on `userData.archdiscStudioCCQuads` so repeated runs are exact; the BufferGeometry is rebuilt with triangulated indices + `computeVertexNormals()`. New ops `__studioCatmullClark` (levels 1-4), `__studioCatmullClarkMarkCrease(uuid, vA, vB, sharpness)`, `__studioCatmullClarkClearCreases`, `__studioCatmullClarkGetStats`. Auto-registered via `v3/catmullclark/`. Existing `rhinosubd/catmull.js` (triangle-based) stays untouched. |
| Grasshopper (visual node graph) | DONE | slice 759 — the geometry node-graph editor (slice 152) supplies the Grasshopper-style DAG runtime; the visual graph runner (slice 700, `v3/ghgraph/`) handles cross-op dataflow; on top of those a dedicated Rhino-flavour NURBS palette ships in `v3/ghnurbs/` with the seven canonical Grasshopper NURBS components — **NurbsCurve** (P0..P3 control points + degree), **NurbsSurface** (4×4 control net + degreeU/degreeV), **NurbsExtrude** (curve + direction + distance), **NurbsLoft** (two curves + samples — Coons-style ruled skin), **NurbsRevolve** (curve + axis + sweep angle, Rodrigues rotation), **NurbsTrim** (surface + closed UV-space boundary polyline + keepInside flag, ray-cast point-in-polygon quad cull), **NurbsOffset** (curve + signed distance, parallel-transport-framed tube). Each node is `{ kind, params, evaluate(inputs) }` returning a real `THREE.Mesh` via inline Cox-de-Boor B-spline basis evaluation (same recursion as `v3/nurbs/bspline.js`), tagged with `userData.archdiscStudioPrimitive` so the existing Studio surface (selection / outliner / xform / save-scene) treats it as a normal primitive. Ops `__studioGHNurbsCreateCurve` / `CreateSurface` / `Extrude` / `Loft` / `Revolve` / `Trim` / `Offset` / `ListNodeKinds` auto-register via `v3/ghnurbs/autoload.js` wired into `api.js`. |

## SketchUp

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Push/Pull face extrude | DONE | slice 747 — real SketchUp `PushPullTool.cpp` single-face surgery (`modeling/pushPull.js`). Coplanar BFS (~0.5° tolerance, dot ≥0.99996) groups a quad's two triangles into one logical face; edge-shared neighbours flood-fill into a region; a boundary-loop walk picks the one-use edges and chains them; cap verts are duplicated, pushed along the face normal by `distance`, re-emitted with the duplicated indices preserving winding; real side quads (two tris each) along the boundary. Bottom face stays put; volume grows by exactly `faceArea × distance`. Interactive begin/apply/commit handle so drag updates mutate cap verts in place. Welds non-indexed BoxGeometry first (BufferGeometryUtils.mergeVertices fallback included). New ops `__studioPushPullFace` (headless one-shot) / `__studioPushPullStart` / `__studioPushPullUpdate` / `__studioPushPullCommit` / `__studioPushPullCancel` / `__studioPushPullState`. Auto-registered via the `v3/pushpull/` autoload chain. |
| Inference snapping | DONE | slice 748 — real SketchUp-style live inference engine (`v3/snap/inference.js`). Per-cursor query resolves the highest-priority snap among endpoint (green `0x1de989`), midpoint (cyan `0x1de9e9`), face centre (cyan), on-edge (red `0xff3344`), on-face (blue `0x3399ff`), and red/green/blue axis guides projected through the last anchor. SketchUp's snap-priority order is honoured (endpoint > intersection > midpoint > centre > on-edge > on-face > axis > parallel/perp). Per-mesh feature cache (vertices + edge midpoints + face centroids) is `position.version`-stamped and `WeakMap`'d so a topology edit invalidates only when the geometry changes; otherwise queries are pure projection + screen-distance tests. Axis-lock clamps the returned `point` onto the locked axis line through the anchor (`__studioInferenceLock('x'|'y'|'z')`). Pixel tolerance is configurable. New ops: `__studioInferenceQuery` / `Enable` / `Lock` / `SetAnchor` / `SetTolerancePx` / `GetState` / `GetQueryCount`. Auto-on via `v3/snap/` autoload. The visible SVG overlay marker + dashed construction line is a future UI slice — the engine itself is the parity-closing piece. |

## MagicaVoxel (voxel modelling)

| Capability | Status | Notes / next |
|------------|--------|--------------|
| Single-voxel paint | DONE | `voxel/` volume (Uint8 grid + palette) with set/get/clear, greedy-merged scene mesh, OBJ/PLY/JSON export, paint-on-click. |
| Bulk shape brushes (box/line/sphere/fill) | DONE | slice 741 — `voxel/index.js` gains four MagicaVoxel/Goxel-style bulk ops, each rebuilding the merged mesh once: `__studioVoxelBox` (axis-aligned filled box between corners, idx 0 erases), `__studioVoxelLine` (3-D Bresenham), `__studioVoxelSphere` (solid (r+0.5)² ball), `__studioVoxelFill` (6-connected flood-fill bucket). e2e: box=64, line=11, sphere=389 centred/symmetric, fill=32768, scene mesh 432 tris. |

## Interop (all tools)

| Capability | Status | Notes / next |
|------------|--------|--------------|
| glTF / OBJ import | DONE | real GLTFLoader / OBJLoader (slice 145) |
| glTF export | DONE | GLTFExporter |
| FBX / USD import | DONE | Autodesk FBX (three FBXLoader) + Pixar USD/USDZ (three USDZLoader) wired into the import pipeline + ribbon buttons (slice 162); real battle-tested loaders |
| USD layer composition | DONE | slice 761 — pure-JS Pixar USD layer-stack composer (`v3/usdlayer/`). `Layer { name, prims, sublayers, references, variants }` + `LayerStack { rootLayer, layers }` + `composeStack(rootLayer)` that walks the stack in strength order (parent local opinions strongest, then variant selection, then sublayers in declaration order with `sublayers[0]` strongest, then reference arcs remapped under the reference target path) and merges per-{primPath,attrKey} opinions with first-writer-wins (i.e. stronger opinion wins).  Cycle-guarded by layer name; deterministic sorted output.  `usdaSerialize.js` emits the canonical `#usda 1.0` text format from a composed prim tree (nested `def Xform "name" { … }` blocks, value typing for `double` / `double2` / `double3` / `double4` / `bool` / `string` / `string[]` / `double[]`, deterministic key-sorted layout).  No Pixar libraries — pure JS, no new deps.  Ops: `__studioUSDLayerCreate` / `AddPrim` / `AddSublayer` / `AddReference` / `AddVariant` / `SetVariantSelection` / `Compose` / `ExportUSDA` / `List` / `Reset`.  Auto-registered under the `usdlayer` palette category.  Headed e2e (`studio-usd-layer-electron.spec.js`) creates a base + override layer with conflicting `xformOp:translate` on `/World/Box`, parents override as the strongest sublayer of base, composes the stack → override's `(2,0,0)` translate wins (base's `(1,0,0)` masked), and the exported USDA contains the override value verbatim; 5-cam sweep for remote-desktop verification. |

## Caveat

Honest tracker, not a literal claim of feature-complete parity. Many entries are
PARTIAL/STUB/ABSENT by design — this map exists so the gaps are explicit and
trackable as they are closed, slice by slice.
